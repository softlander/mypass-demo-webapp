const Config = {
    backend_url: "https://demo.mypass.id",
    backend_api_namespace: "/api",
    backend_ws_namespace: "/socket.io",
    provider: "https://nodes.devnet.iota.org:443",
    keyId: "demo-myPassID"
};

export default Config;
